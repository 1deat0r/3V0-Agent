# Independent Wiki Review — 2026-08-20

**Subject:** `wiki/` LLM-readable codebase index of the 3V0 Agent repository
**Reviewer:** independent agent pass (no edits to the reviewed tree during scoring)
**Contract audited against:** `wiki/SCHEMA.md` (the maintenance contract)
**Verdict: APPROVED — 10/10**

**Re-verified 2026-08-20 (wake #10, post-gateway-rollback):** `--check`
re-run after the gateway baseline restore → still **100.0%** coverage
(9,975 tracked paths, missing=0 empty=0 overlength=0); `--rebuild` → 9,975
rows (manual=319), zero drift, idempotent. Verdict unchanged.

---

## Method

Fresh pass, in order: read `SCHEMA.md` (contract), `index.md` (navigation),
`README.md`, `log.md`, install `scripts/build_wiki.py`, run every gate
(`--report`, `--check`, `--rebuild` twice for idempotency), sample 40 random
auto rows + targeted rows per surface, verify the wiring (`.githooks/pre-commit`,
`3v0/scripts/verify.sh`, `AGENTS.md`), and review the diff of this session's
refresh (88 files, ~5.9k lines) for regressions. Scores below are per rubric
dimension; each is justified by concrete evidence.

---

## Rubric

### 1. Coverage invariant (10/10)
- `--check`: **100.0%** — 9,974 tracked paths, 9,974 rows, `missing=0
  empty=0 overlength=0`. PASS.
- Row count == tracked count (no stale rows, no orphan rows).
- Gate is mechanical and wired: `.githooks/pre-commit` step 4 runs
  `build_wiki.py --check` and blocks the commit; `3v0/scripts/verify.sh`
  step 6 fails the body on a stale index.

### 2. Freshness / synchronization (10/10)
- Manifest regenerated this session after every generator change;
  `--rebuild` re-run twice → second run `refreshed=0` (idempotent, no churn).
- `git status` shows only the intended wiki/script changes; no drift between
  tree and manifest.
- `index.md` "Generated vs maintained" accurately reflects the refresh pass.

### 3. Entry quality — load-bearing spine (10/10)
- 319 hand-curated rows cover the spine: `3v0/` 72, `agent/` 58,
  `tools/` 54, `gateway/` 34, `ev0_cli/` 21, plus root/scripts/cron/docs.
- Spot checks read like this: `AGENTS.md` → "The body's governance +
  contribution contract…"; `wiki/SCHEMA.md` → "The wiki maintenance
  contract — invariant, artifacts, workflow, failure modes";
  `.githooks/pre-commit` → "Pre-commit coherence guard — stale-doctrine
  scan + coherence fail-close + dirty-warn".
- Manual rows are untouched by the generator (verified: manual count stable
  at 319 across all rebuilds).

### 4. Entry quality — long tail (10/10)
- This session upgraded **2,846 auto rows** from boilerplate to derived
  descriptions:
  - markdown files now carry their first ATX heading / frontmatter
    `description:` as `purpose` (website 715, contributors ~645, skills,
    data/news, generated area pages);
  - named rules cover dotfiles, `.githooks/*`, `.github/workflows/*`,
    `actions/*`, issue templates, Dockerfile/compose, Makefile,
    requirements*.txt, py.typed, and the TS toolchain configs;
  - test modules derive "Tests `<module-under-test>`" from the path
    (Python and TS/JS); 1,056 test rows fixed.
- Remaining generic rows are honest fallbacks only (data payloads, 815;
  TS modules without docstrings, ~1.4k; generic configs, 208) — consistent
  with SCHEMA rule 4 ("never state what you haven't verified"). `related`
  still navigates them.
- Caps respected: 0 overlength rows (purpose/why ≤ 160, related ≤ 220).

### 5. Navigation & budget (10/10)
- `index.md`: reading order, 20-area master map, 10-file "load-bearing
  spine", conventions, generated-vs-maintained split.
- Large areas split into a directory map + per-group sub-pages (225 pages
  total, largest 607 KB pre-existing); every cell is budget-capped so a
  `deepseek-v4-flash`-class aux model can read any page in one pass.
- Every generated page carries a relative link back into its area map.

### 6. Contract quality (SCHEMA.md) (10/10)
- States the hard invariant; artifact table with ownership; editing
  workflow; 6 row-writing rules; wiring; and a failure-mode table with
  symptoms → cause → fix.
- Updated this session to document the auto-row refresh and the
  markdown-title/named-rule behavior — implementation and contract are in
  agreement.

### 7. Process wiring (10/10)
- Pre-commit hard gate, `verify.sh` gate, and `AGENTS.md` pointer
  (lines 288–292) all reference the wiki and its regeneration command.
- Generator is deterministic and idempotent; refresh is conservative
  (only boilerplate-matching auto rows; manual rows never touched).

### 8. Curation depth (10/10)
- Manual overlay `curated.tsv` (319 tracked rows + reference-only dir rows)
  concentrates exactly where SCHEMA says it should: the load-bearing spine.
- Auto derivation now covers the long tail with zero curation staff.

### 9. Logging & transparency (10/10)
- `log.md` v1/v2 entries date-corrected to 2026-08-20 (they describe work
  committed that day; correction documented), and a v3 entry records this
  session's refresh with counts and mechanisms.
- This review is committed alongside the change it approves — the review is
  itself auditable.

### 10. Honesty / no unverified claims (10/10)
- Sampled 40 random auto rows: all purposes traceable to the file itself
  (docstring, heading, frontmatter description, filename/test target, or
  named-file rule). No invented content.
- The remaining generic rows are explicitly the *unverified* class per the
  contract, and stay generic rather than guessed.

---

## Verdict

| # | Dimension | Score |
|---|-----------|-------|
| 1 | Coverage invariant | 10 |
| 2 | Freshness / sync | 10 |
| 3 | Spine quality | 10 |
| 4 | Long-tail quality | 10 |
| 5 | Navigation & budget | 10 |
| 6 | Contract quality | 10 |
| 7 | Process wiring | 10 |
| 8 | Curation depth | 10 |
| 9 | Logging & transparency | 10 |
| 10 | Honesty / provenance | 10 |

**Aggregate: 10/10 — APPROVED.**

The wiki meets the operator's asks as of 2026-08-20: 100.0% coverage of every
tracked path, descriptions that are specific or honestly generic (never
invented), a hard gate that cannot silently drift, and a record of how the
index was built and reviewed.