The waist of the system. `run_agent.py` is the conversation loop, `cli.py` the interactive shell, `model_tools.py`+`toolsets.py` the tool orchestration, `threev0_state*.py` the session store, `threev0_constants.py`/`threev0_logging.py` the profile-aware paths. Start here to trace any feature end-to-end.
