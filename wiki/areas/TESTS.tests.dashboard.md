# tests/ + 3v0/tests/ — the Python test suites (tests-js/ and evals/ pruned) — `tests/dashboard/`

Auto-rendered from `wiki/manifest.tsv`.
Columns: path · kind · purpose · why · related

| path | kind | purpose | why | related |
|------|------|---------|-----|---------|
| `tests/dashboard/test_ws_client_host.py` | test | Regression tests for the in-container WebSocket client host resolution. | Test module — asserts the repo contract; run via scripts/run_tests.sh | tests/dashboard/ |
