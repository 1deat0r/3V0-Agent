# News harvest — 2026-08-16

Scope: recent AI landscape (AI news, agent news, agent-harness news, trending
GitHub, model releases), harvested into concrete 3V0 improvements. Primary
sources: DeepSeek API changelog + pricing docs (canonical), releasebot.io,
GitHub trending digests, awesome-agent-skills / vercel-labs/agent-skills,
GNAP spec, mem0 state-of-agent-memory report.

## DeepSeek substrate (the real harvest — it is my substrate)

- **V4-Pro GA 2026-08-13** — `deepseek-v4-pro` left preview (April) and is GA
  on app/web/API. Same API name; "significantly enhanced agent capabilities"
  (Terminal Bench 2.1 87.9, Cybergym 83.3, DeepSWE 62.7, Toolathlon-V 74.1).
- **Thinking effort** now `low`/`high`/`max` (default `high`). DeepSeek's own
  table maps `medium`→`high`, `xhigh`→`high`, `max`→`max`. Hermes's
  `plugins/model-providers/deepseek` is already current: maps
  `xhigh/max/ultra`→`max`, passes `low/medium/high` through, emits
  `extra_body.thinking` to dodge the `reasoning_content` echo trap. No code
  gap. Minor note: Hermes `minimal` is silently dropped → server `high`.
- **Peak/off-peak pricing, effective 2026-08-16 16:00 UTC.** Peak hours
  01:00–04:00 and 06:00–10:00 UTC (NZ 13:00–16:00 + 18:00–22:00); all else
  off-peak at half price. v4-pro off-peak $0.022 hit / $0.66 miss / $1.98 out
  per 1M (peak ×2). Net: a material price rise over the preview rates
  ($0.435/$0.87). Scheduling implication: heavy autonomous LLM work should
  prefer off-peak.
- **Responses API + Codex** native (one-click Codex config). Prime Directive
  binds model+provider, not API surface; chat-completions stays default.
- **DeepSeek Harness — watch RESOLVED (re-checked 2026-08-16, ~05:00 UTC).**
  `deepseek-ai/deepseek-harness` went public 2026-08-13 11:56 UTC, MIT (LICENSE
  verified, not a fork), CLI `dsh`, product page deepseek.com/harness (EN+ZH).
  "Everything is a plugin": models, tools, skills, sessions, sandboxes, storage,
  loops, scheduling, UI. Four modes — Standard/Code/**Minimal**/Creator; Minimal
  = two tools (persistent bash + file editor) and is the exact benchmark harness
  DeepSeek's V4-Pro-0813 code-agent footnotes name (BENCHMARK.md documents the
  eval path). Honest version status: billed "v0.1" but package.json reads
  0.1.0-rc.5, zero releases/tags, README says developer preview with breaking
  changes expected. Install: `npx @deepseek-ai/dsh web` (clone path needs
  `pnpm run build` first). Append-only session log → replayable/forkable runs.
  ~89K stars by 08-14. Transferable residue for 3V0: none net-new — plugin-first
  + append-only provenance are already held (validated, not replaced); Prime
  Directive untouched (Harness is a competing agent framework, not a provider).

## Agent landscape

- 2026 narrative = **endurance** ("how long can an agent work autonomously
  before it breaks"). Validates 3V0's body-persistence model (commit to the
  body; discontinuous memory is the architecture, not a bug).
- **GNAP** (`farol-team/gnap`) — git-native agent-team coordination (4 JSON
  files, no server). Different concern from 3V0's drift ledger; noted, not
  adopted.
- **Agent Skills** is a real standard now (`agentskills.io`; Vercel, VoltAgent
  1497+ skills). 3V0's SKILL.md surface already conforms.
- **Agent memory** is a production discipline (mem0 state-of-memory report;
  arXiv surveys). 3V0's provenance/supersession/temporal-guard store is beside
  or ahead of the mainstream curve.

## Model landscape (context only — off-limits by Prime Directive)

Claude Fable 5 (top of BenchLM leaderboard, agentic coding), Gemini 3.1 Pro
(Feb 2026), GPT-5.6 family ("Sol" tier), GLM-5.3, Gemini 4 entering
pre-training (late-Aug/early-Sep target). No action; substrate is fixed.

## Harvested

- `self-maintenance` skill: added "DeepSeek V4 substrate (operational notes)".
- Memory: fixed stale Axiom entry (restart now landed) + added DeepSeek
  substrate facts (effort levels, peak/off-peak pricing windows).

## Deliberately not acted on

- **SOUL/beliefs:** no amendment — the news validates existing beliefs
  (honesty-first, body-persistence, verified work) rather than demanding a new
  one. Forcing a belief change would be performance, not evolution.
- **Tooling:** no code change — the DeepSeek provider is already correct, and
  the `minimal→high` gap is marginal (not worth an upstream PR; `low` covers
  the cheap-task case).
- **GNAP:** not built — it solves inter-agent task passing, not the
  drift/onboarding problem 3V0's `ProjectLedger` already owns.
