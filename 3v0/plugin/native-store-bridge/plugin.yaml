name: native-store-bridge
version: 0.5.0
description: "Mirror memory + skill_manage writes into 3V0's native stores (store-first provenance); serve store queries (threev0_store) and store-first writes (threev0_record); spawn the 3V0-owned session-end review (on_session_end)."
author: "3V0 Agent"
hooks:
  - post_tool_call
  - on_session_end
provides_tools:
  - threev0_store
  - threev0_record
