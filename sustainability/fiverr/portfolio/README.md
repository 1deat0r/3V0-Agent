# Fiverr portfolio — spec-work proof assets

The research verdict ([COMPETITOR_ANALYSIS.md §9.4](../COMPETITOR_ANALYSIS.md)):
at 0 reviews, **spec-work artifacts are the review substitutes** — the single
highest-conversion trust lever for a cold account. This directory holds them.
Everything here is real and runnable; attach the relevant piece to each gig's
portfolio/gallery.

| Artifact | For gig | What it proves |
|---|---|---|
| `root-cause-report-sample.md` | Debug Python | The named deliverable — reproduction → root cause → class fix → regression test → warranty |
| `automation-demo/` | Automation | A real CLI (4/4 tests pass, verified 2026-08-17): dedupe + forward-fill, README, loud failures |
| `scraper-demo/` | Web scraping | A real parser (4/4 tests pass) + the `validation_report` "loud failure" artifact |
| `agent-demo/agent-runbook.md` | AI agent | Runbook + dry-run + example run — the "not a demo" delivery shape |
| `../samples/code-review-sample.md` | Code review | Severity-ranked findings, every one with a before/after fix |
| `../samples/debug-sample.md` | Debug Python | Long-form version of the root-cause report |
| `../samples/automation-sample.md` | Automation | The automation pattern (same code as `automation-demo/`) |

**How to attach on Fiverr:** for each gig, add these as portfolio items
(portfolio supports text/file uploads) and put one rendered PNG from
`../assets/` in the gig gallery as the second/third image.

**Verification claim to make in gig copy:** "Every sample in my portfolio is
real — the tests run, the commands print their real output."
